Install dependencies: python, npm, node.js, etc.
* navigate into /dev
* pip install flask
* navigate into /client
* npm install

To run:
* navigate into /dev
* python server.py
* navigate into /client
* npm run dev